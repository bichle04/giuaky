# README

## Mô tả

Chương trình `ls` được cài đặt để hỗ trợ các tùy chọn sau: -A, -a, -c, -d, -F, -f, -h, -i, -k, -l, -n, -q, -R, -r, -S, -s, -t, -u, -w.

## Hướng dẫn sử dụng

Biên dịch chương trình bằng cách chạy lệnh `cmake` và `make` trong thư mục chứa CMakeLists.txt.

Chạy chương trình với các tùy chọn tương tự như lệnh `ls` thông thường.
